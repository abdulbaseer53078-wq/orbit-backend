services:
  - type: web
    name: orbit-backend
    env: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: PORT
        value: 5000
      - key: JWT_SECRET
        generateValue: true
      - key: STELLAR_NETWORK
        value: testnet